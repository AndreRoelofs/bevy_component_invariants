# Nothing in the crate needs nightly — it builds, lints and tests clean on
# stable. The pin is here to keep nightly-only tuning available.
[toolchain]
channel = "nightly"
# `rust-src` is not used by the build; rust-analyzer wants it for std.
components = ["rustfmt", "clippy", "rust-src"]
profile = "minimal"
